/**
 * PostCSS Configuration
 * Configures PostCSS plugins for CSS processing in the project.
 * 
 * Features:
 * - Tailwind CSS integration
 * - Autoprefixer for cross-browser compatibility
 * 
 * Plugins:
 * - tailwindcss: Processes Tailwind CSS directives and utilities
 * - autoprefixer: Automatically adds vendor prefixes to CSS rules
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
